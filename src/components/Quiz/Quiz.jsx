import React from 'react';
import PropTypes from 'prop-types';
import classes from './Quiz.module.scss';

export default function Quiz({}) {
  return <div className={classes.QuizContainer}></div>;
}

Quiz.propTypes = {};
